import type { GetStaticProps, NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import { useEffect } from 'react'
import { start } from 'repl'
import styles from '../styles/Home.module.css'
import {Result, RootObject} from "../types"

//https://www.themuse.com/developers/api/v2
// export const getStaticProps: GetStaticProps = async (context) => {
//   const API = "https://www.themuse.com/api/public/jobs";
//   const res = await fetch(API + '?category=Software%20Engineering&location=Seattle%2C%20WA&page=3&api_key=' +
//    process.env.THE_MUSE)
//   const jobs: RootObject = await res.json();
//   return {
//     props: {
//       results: jobs.results
//     }
//   }
// }

// async function statusCheck(res: object) {
//   if (!res.ok}) {
//     throw new Error(await res.text());
//   }
//   return res;
// }

function hi() {
  console.log("hi")
}

const Home: NextPage <{results: Result[]}> = ({results}) => {

  return (
    <div className={styles.container}>
      <Head>
        <title>Job Relevance Evaluator</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className='flex flex-col items-center'>
        <p className='text-center w-5/12 my-7'>Have you ever wondered how relevant your technical skills are in the job market?
         The Job Relevance Evaluator will look up job postings in your selected field and evaluate how
         relevant it is.</p>
        {/* {results.map((job: any) => {
          return <li key={job.id}>{job.contents}</li>
        })} */}
        <div id="start-btn" className='w-60 my-7 border-2 rounded-md bg-sky-500 hover:bg-sky-700 hover:cursor-pointer' onClick={hi}>
          <p className='p-4 text-white'>Get Started</p>
        </div>
        <div className='w-60 my-7 border-2 rounded-md bg-sky-500 hover:bg-sky-700 hover:cursor-pointer'>
          <p className='p-4 text-white'>About</p>
        </div>
      </main>
    </div>
  )
}

export default Home
